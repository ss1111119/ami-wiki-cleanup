rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 只有登入的用戶可以讀寫
    match /translations/{articleId} {
      allow read, write: if request.auth != null;
      match /sections/{sectionId} {
        allow read, write: if request.auth != null;
      }
    }
  }
}
